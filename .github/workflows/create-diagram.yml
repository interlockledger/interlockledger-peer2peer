name: Create Diagram

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2.3.4

    - name: Repo Visualizer
      uses: githubocto/repo-visualizer@0.5.0
      with:
          # A list of paths to exclude from the diagram, separated by commas.
          excluded_paths: .github,.editorconfig,.gitattributes,.gitignore,.nuget.config
